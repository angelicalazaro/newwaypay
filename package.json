{
  "name": "newwaypay-angularjs",
  "version": "1.0.0",
  "description": "NewWayPay — Application bancaire AngularJS 1.x (legacy, avant migration Angular 21)",
  "scripts": {
    "start": "npx http-server . -p 8080 -o",
    "serve": "npx http-server . -p 8080"
  },
  "devDependencies": {
    "http-server": "^14.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}
